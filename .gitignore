/target

# Per-crate / per-program build dirs (Anchor program, devnet-demo crate,
# etc.). They're regenerated by `anchor build` / `cargo build`.
**/target

# The deploy keypair authorises program upgrades on whichever cluster
# the program is deployed to. Treat as a credential — never commit.
programs/target/deploy/*-keypair.json
