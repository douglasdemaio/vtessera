//! Empty crate root — all tests live in tests/adversarial.rs.
//!
//! See that file for the suite. The `[workspace]` table in Cargo.toml
//! makes this crate its own root so the solana-sdk 2.1 / litesvm lock
//! never mixes with the host or anchor workspaces.
