[package]
name = "vtessera-relay"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Vtessera relay — TCP reverse tunnel for nodes behind NAT"

[[bin]]
name = "vtessera-relay"
path = "src/main.rs"

[dependencies]
vtessera-mini-http = { path = "../mini-http" }
serde = { workspace = true }
serde_json = { workspace = true }
rand = { workspace = true }
base64 = "0.22"
