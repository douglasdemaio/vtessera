[package]
name = "vtessera-x402-client"
description = "AI-agent x402 client for Vtessera: pays the escrow on devnet and submits a job to a vtessera-node"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/douglasdemaio/vtessera"

# Excluded from the host Cargo workspace (see /home/douglas/vtessera/Cargo.toml).
# Pinning solana-sdk 1.18.x drags in an older crypto dep tree
# (curve25519-dalek 3 / zeroize 1.5) that conflicts with the host crates'
# ed25519-dalek 2 ecosystem — same reason `crates/devnet-demo` lives out of
# the workspace. This crate carries its own Cargo.lock.

[[bin]]
name = "vtessera-x402-client"
path = "src/main.rs"

[dependencies]
# Solana SDK pinned to the 1.18 line — matches crates/devnet-demo and what's
# cached on the dev box. Bump together with the on-chain program toolchain.
solana-client = "=1.18.26"
solana-sdk = "=1.18.26"
spl-token = { version = "4.0.0", features = ["no-entrypoint"] }
spl-associated-token-account = { version = "2.3.0", features = ["no-entrypoint"] }

# Anchor uses borsh 0.10 for IX serialisation on the 0.30 line.
borsh = "0.10"
sha2 = "0.10"
